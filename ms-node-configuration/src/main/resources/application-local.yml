server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/turkuazsengul/micro-service-config.git
          search-paths: config
          try-master-branch: true
        default-label: master